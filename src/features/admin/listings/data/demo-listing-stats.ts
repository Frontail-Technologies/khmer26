import type { AdminListingStats } from "../types"

export const DEMO_ADMIN_LISTING_STATS: AdminListingStats = {
  active: 48290,
  pending: 14,
  flagged: 8,
  rejected: 162,
  submittedToday: 324,
  avgModerationMinutes: 45,
}
